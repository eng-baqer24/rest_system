{
  "name": "rest-system",
  "private": true,
  "description": "جذر المستودع — التطبيق الفعلي في مجلد renaissance",
  "scripts": {
    "install": "cd renaissance && npm install",
    "build": "cd renaissance && npm run build"
  }
}
